# catalog-runtime

Runtime catalog metadata for DEVONthink MCP tool routing.

Contents:
- `registry/`: tool/group/alias/overlap/broker JSON
- `schema/`: JSON Schemas used to validate registry files

These files are consumed as runtime metadata and can be validated with `jsonschema`.
