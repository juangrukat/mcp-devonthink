# Client Connector Templates

This folder contains thin stdio connector templates for:
- VS Code
- Codex
- Claude Code

Use `../scripts/install-connectors.sh` to generate path-correct files in `clients/generated/`.
